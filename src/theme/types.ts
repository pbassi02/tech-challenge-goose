const gridBase = 4;

export enum GridVariant {
  base2 = gridBase * 0.5,
  base4 = gridBase,
  base8 = gridBase * 2,
  base12 = gridBase * 3,
  base16 = gridBase * 4,
  base20 = gridBase * 5,
  base24 = gridBase * 6,
  base28 = gridBase * 7,
  base32 = gridBase * 8,
  base36 = gridBase * 9,
  base40 = gridBase * 10,
  base48 = gridBase * 12,
  base64 = gridBase * 16,
}
