// import { lightTheme } from './light';
import {lightTheme} from './lightTheme';

// Once a dark theme is available it can be switched here based on some logic
const theme = lightTheme;

export default theme;
